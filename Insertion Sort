import java.util.*;
public class Main
{
	public static void main(String[] args) {
		int arr[]={12,39,40,56,78};
		int i,j,key,len=arr.length;
		for(i=1;i<len;i++){
		    key=arr[i];
		    j=i-1;
		    while(j>=0 &&arr[j]>key){
		      arr[j+1]=arr[j];
		      j--;
		    }
		    arr[j+1]=key;
		}
		for(int X:arr)
		System.out.print(X+" ");
	}
}
